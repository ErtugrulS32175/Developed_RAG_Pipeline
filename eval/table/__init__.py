"""Table extraction measured against hand-built ground truth.

Structure and values are scored separately because they fail separately: a
table can have every number right and the wrong shape, or the right shape with
a misread digit.
"""
