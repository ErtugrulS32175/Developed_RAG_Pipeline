"""Turning a page or an image into structured content.

The table path: routing to a backend, the backends themselves, and the
orchestration around them (preprocessing, header canonicalisation, export).
A document that is native text skips almost all of it; a scanned table goes
through the whole thing.
"""
