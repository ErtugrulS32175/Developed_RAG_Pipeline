"""The HTTP surface: the service itself and the OpenWebUI chat adapter.

Named api rather than serving because the repository root already has a
services/ folder holding the model microservices, and two near-identical names
in one tree is a trap.
"""
